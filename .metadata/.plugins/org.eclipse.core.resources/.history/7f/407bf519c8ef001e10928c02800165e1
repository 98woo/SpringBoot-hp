$().ready(function () {
	$("#uploadExcelfile").on("click", function () {
		$("#excelfile").click();
	});
	
	/* 파일이 선택되면 수행해라 */
	$("#excelfile").on("change", function () {
		// 선택된 파일의 정보를 출력
		var files = $(this)[0].files;
		console.log(files);
	});
});